# Core Daemon & Processing Dependencies
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=9.0.0

# PyTorch (Install with appropriate CUDA variant from pytorch.org if not present)
torch>=2.1.0
torchvision>=0.16.0

# MoGe inference-chain deps (imported at model-load time via moge/model/v2.py
# and moge/utils/geometry_numpy.py; missing ones surface as misleading
# 'Model version "vX" not found' because MoGe masks ModuleNotFoundError)
huggingface-hub>=0.20.0
scipy>=1.8.0

# MoGe Upstream Dependencies (NOT on PyPI under these names — install from git.
# Pins mirror MoGe's own pyproject and the proven local .venv: do not replace
# with bare "flex-gemm"/"utils3d", pip can never resolve those.)
flex-gemm @ git+https://github.com/JeffreyXiang/FlexGEMM.git@b2fadb29d41846c7981ade6801ffc689fae119cf
utils3d_moge @ git+https://github.com/EasternJournalist/utils3d-moge.git@62f09d58509485564e24d5d9f6aac9ee9ebc0c37

# Testing
pytest>=7.0.0
